apiVersion: v1
kind: Service
metadata:
  name: hotstar-service
  labels:
    app: hotstar
spec:
  type: LoadBalancer
  selector:
    app: hotstar
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
